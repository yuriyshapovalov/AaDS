using System;

namespace AaDS.Sorting
{
    /// <summary>
    /// _Burst sort algorithm
    ///  Θ(n) ~ ____
    /// </summary>
    public class BurstSort<T> : ISort<T> where T : IComparable, new()
    {
        public T[] Sort(T[] array)
        {
            // not implemented
            return array;
        }
    }
}
