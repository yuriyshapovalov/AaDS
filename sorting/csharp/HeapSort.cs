using System;

namespace AaDS.Sorting
{
    /// <summary>
    /// Heap sort algorithm
    ///  Θ(n) ~ ____
    /// </summary>
    public class HeapSort<T> : ISort<T> where T : IComparable, new()
    {
        public T[] Sort(T[] array)
        {
            // not implemented
            return array;
        }
    }
}
