# bubble sort implementation

RandSequence <- sample(1:1000, 1000, replace=T);

bubbleSort <- function(sequenece) {
    clear <- TRUE;
    while(clear) {
        clear <- FALSE;
    
        for(i in sequence) {
        
            
        }
    }
}

